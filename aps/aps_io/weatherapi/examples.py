from weatherapi_request import subjective_forecast_latest, subjective_forecasts_available, radar, radar_accumulated


# avail = subjective_forecasts_available()

radar_accumulated()


# resp = subjective_forecast_latest()
# with open(r"./output/subjective_map.png", "wb") as f:
#     f.write(resp.content)

a = 1